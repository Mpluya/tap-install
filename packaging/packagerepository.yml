apiVersion: v1
items:
- apiVersion: packaging.carvel.dev/v1alpha1
  kind: PackageRepository
  metadata:
    annotations:
      kapp.k14s.io/change-rule.create-order: "upsert before upserting tap-package"
      kapp.k14s.io/change-rule.delete-order: "delete after deleting tap-package"  
    name: tanzu-tap-repository
    namespace: tap-install
  spec:
    fetch:
      imgpkgBundle:
        image: index.docker.io/tapsme/tap-packages:1.1.0
        secretRef: 
          name: tap-install-gitops-regcred