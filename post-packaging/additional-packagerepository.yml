apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageRepository
metadata:
  name: tap-additional-package-repo
  namespace: tap-install
spec:
  fetch:
    imgpkgBundle:
      image: index.docker.io/tapsme/more-ootb-supply-chains-repo-bundle:1.0.0
      secretRef: 
        name: registry-credentials
        namespace: tap-install