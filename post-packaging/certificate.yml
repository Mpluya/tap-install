apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tap-gui-cert
  namespace: tap-gui
spec:
  commonName: tap-gui.learn.tapsme.org
  dnsNames:
  - tap-gui.learn.tapsme.org
  issuerRef:
    kind: Issuer
    name: selfsigned-issuer
  secretName: tap-gui-cert-secret